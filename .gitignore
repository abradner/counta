# --- Universal ---
.DS_Store
Thumbs.db
*.log
tmp/
*.swp
*.swo

# Local agent config (per-machine permissions — never shared)
.claude/settings.local.json

# Env files — commit templates (.env.template*), never real values
.env
.env.*
!.env.template
!.env.template.*

# IDE local state (uncomment if unwanted in this repo)
# .idea/
# .vscode/

# --- Ruby/Rails ---
/log/*
/storage/*
/public/assets
.bundle/
vendor/bundle
