<%# Rendered once, outside every hidden screen section, so it's visible
    whichever surface (landing, unlock, recovery, app, account) is currently
    shown — avoids duplicating this markup per screen. %>
<footer class="attribution"><%= t('ui.footer.attribution_html').html_safe %></footer>
