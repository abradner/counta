<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "counta.click" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Counta">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="webauthn-rp-id" content="<%= Rails.configuration.x.webauthn_rp_id %>">
    <meta name="signed-in" content="<%= current_account.present? %>">
    <%# Gates window.countaTest, the specs' encrypt/decrypt probe. Test env only. %>
    <% if Rails.env.test? %><meta name="test-hooks" content="true"><% end %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# public/icon.svg is canonical; the PNGs are rendered from it by
        `rake icons:render`. iOS can't use an SVG for apple-touch-icon, which
        is why the rasters exist at all — and it wants 180px specifically. %>
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="icon" href="/icons/icon-32.png" sizes="32x32" type="image/png">
    <link rel="apple-touch-icon" href="/icons/icon-180.png" sizes="180x180">

    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>
  </head>

  <%# Client-side copy travels as a data attribute, not an inline <script>:
      the CSP has no 'unsafe-inline', and it must be present before first
      render so a fetch won't do. See app/javascript/i18n.js. %>
  <body data-i18n="<%= I18n.t('client').to_json %>">
    <%= yield %>
  </body>
</html>
