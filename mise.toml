# Toolchain pins — run `mise install` before working in this repo.

[tools]
ruby = "4.0"

# [env]
# Per-project env vars and port offsets go here if several projects run
# concurrently on one machine. Never put required production config here —
# required config fails fast at boot (see AGENTS.md §6), it doesn't get
# defaults in a checked-in file.
